{
  "name": "rails-migration-guard-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0",
    "@hotwired/stimulus": "^3.2.0",
    "@tailwindcss/postcss": "^4.1.0",
    "cssnano": "^7.0.0",
    "postcss": "^8.5.0",
    "tailwindcss": "^4.1.0"
  }
}
